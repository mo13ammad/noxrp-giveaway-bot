# (c) 2025 ViraUp (viraup.com) - All rights reserved.
# ====== Discord Bot Config (Nox RP Giveaway) ======
DISCORD_BOT_TOKEN=YOUR_TOKEN_HERE

# Optional but recommended for faster slash sync
GUILD_ID=0

# Mandatory: target channel & message (the message everyone must REPLY to)
CHANNEL_ID=123456789012345678
TARGET_MESSAGE_ID=123456789012345679

# Admin roles (comma-separated IDs). Admins are exempt from deletions & quiet hours.
ADMIN_ROLE_IDS=111111111111111111,222222222222222222

# Members with these roles cannot send messages during quiet hours (deleted automatically).
QUIET_ROLE_IDS=333333333333333333,444444444444444444

# Countdown (seconds) and edit tick rate (seconds)
COUNTDOWN_SECONDS=60
TICK_RATE=1.0

# Display-only timezone label
TIMEZONE=Asia/Tehran

# Quiet window (24h format). If START > END, it means it crosses midnight.
QUIET_START=00:00
QUIET_END=09:00

# When countdown reaches this many seconds remaining, bot sends @here alert
ALERT_AT_SECONDS=10

# Only members with these role IDs can participate (reply) in the giveaway channel.
# Leave empty to allow everyone (not recommended).
PARTICIPANT_ROLE_IDS=

# Seconds removed from the active participant's countdown per successful server invite
# detected for them (default 10).
INVITE_BONUS_SECONDS=10

# Optional: bilingual DM content for users without the participant role (registration)
# If REGISTRATION_DM_MESSAGE is set, it's treated as the EN value (backward compatible)
REGISTRATION_DM_MESSAGE_EN=To participate in the giveaway, please register and complete your profile at https://nox-rp.ir/
REGISTRATION_DM_MESSAGE_FA=برای شرکت در قرعه‌کشی، لطفاً در وب‌سایت ثبت‌نام کرده و پروفایل خود را تکمیل کنید: https://nox-rp.ir/

# Optional: bilingual DM content for quiet hours notifications
QUIET_HOURS_MESSAGE_EN=The channel is in quiet hours. Please try again later.
QUIET_HOURS_MESSAGE_FA=کانال در ساعات سکوت است. لطفاً بعداً تلاش کنید.

# Persistent state DB path (SQLite)
STATE_DB_PATH=giveaway_state.db

# Additional bonus seconds when an invited user later obtains a participant role
INVITE_ROLE_BONUS_SECONDS=10

# Minimum account age (in days) for an invited user to be eligible for any invite-based bonus
# Joins from accounts younger than this will not count towards bonuses
INVITE_MIN_ACCOUNT_AGE_DAYS=3

# Embed appearance
# Thumbnail used in embeds (default points to Nox RP icon)
EMBED_THUMB_URL=https://nox-rp.ir/media/site/icon4.png
